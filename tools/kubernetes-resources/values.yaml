# namespace:
#   name: my-namespace
#   labels:
#     env: production
#   annotations:
#     owner: team-x
#   quota:
#     cpu: "4"
#     memory: 8Gi
#     pods: "20"
#     storage: "50Gi"
#     services: "10"
#     pvcs: "10"
#   limits:
#     cpu: 500m
#     memory: 512Mi

# clusterRoles:
#   list-namespaces:
#     rules:
#       - apiGroups: [""]
#         resources: ["namespaces"]
#         verbs: ["get", "list"]

# users:
#   dev:
#     clusterRoles:
#       - cr-list-namespaces
#     namespaces:
#       - name: production
#         roles:
#           - name: view
#             kind: ClusterRole

# groups:
#   admins:
#     clusterRoles:
#       - cluster-admin

# serviceAccounts:
#   ci-deployer:
#     saNamespace: ci                       # namespace, где живёт SA (default: "default")
#     labels:                               # опционально
#       team: platform
#     annotations:                          # опционально (IRSA/WIF и т.п.)
#       eks.amazonaws.com/role-arn: arn:aws:iam::123456789:role/ci
#     imagePullSecrets:                     # опционально (docker pull secret)
#       - name: regcred
#     automountServiceAccountToken: false   # опционально
#     clusterRoles:
#       - cr-list-namespaces
#     namespaces:
#       - name: production
#         roles:
#           - name: edit
#             kind: ClusterRole

# networkPolicies:
#   deny-all-ingress:
#     namespace: production
#     spec:
#       order: 1000
#       selector: all()
#       types:
#         - Ingress
#       ingress:
#         - action: Deny

# globalNetworkPolicies:
#   default-deny:
#     spec:
#       order: 1000
#       selector: all()
#       types:
#         - Ingress
#       ingress:
#         - action: Deny

# clusterPolicies:                        # Kyverno ClusterPolicy (кластерные)
#   require-labels:
#     spec:
#       validationFailureAction: Enforce
#       rules:
#         - name: require-team-label
#           match:
#             any:
#               - resources:
#                   kinds: ["Pod"]
#           validate:
#             message: "Label 'team' is required"
#             pattern:
#               metadata:
#                 labels:
#                   team: "?*"

# policies:                               # Kyverno Policy (namespaced)
#   restrict-image-pull:
#     namespace: production               # default: "default"
#     spec:
#       validationFailureAction: Audit
#       rules:
#         - name: allowed-registries
#           match:
#             any:
#               - resources:
#                   kinds: ["Pod"]
#           validate:
#             message: "Only internal registry is allowed"
#             pattern:
#               spec:
#                 containers:
#                   - image: "registry.example.com/*"

# Поля AppProject (sourceRepos, destinations, orphanedResources и т.д.) через
# values не задаются: AppProject генерирует `argocd-render` из main.yaml stage
# (см. README, секция main.yaml).
